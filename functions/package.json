{
  "name": "poketft-functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "22" },
  "description": "PokéTFT authoritative game loop (#110 Phase 2) — Cloud Functions + Cloud Tasks. A scheduled task fires at each phase deadline and runs the SAME match.ts transition the client uses, then schedules the next. Serverless, scale-to-zero.",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --outfile=lib/index.js --external:firebase-admin --external:firebase-functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "firebase": "^12.14.0"
  }
}
